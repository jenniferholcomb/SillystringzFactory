@{
  Layout = "_Layout";
}

@model Factory.Models.Engineer;

<h3>Edit Engineer,</h3>

@using (@Html.BeginForm())
{
  @Html.HiddenFor(model => model.EngineerId)

  <p>
    Engineer: @Html.EditorFor(model => model.Name)

    <input type="submit" value="Update" />
  </p>
}

<hr>

@Html.ActionLink("Delete Engineer", "Delete", new { id = Model.EngineerId })<br>
@Html.ActionLink("Back to Engineer Details", "Details", new { id = Model.EngineerId })<br>
@Html.ActionLink("Back to Engineers List", "Index")<br>
@Html.ActionLink("Return Home", "Index", "Home")